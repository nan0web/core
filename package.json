{
	"name": "@nan0web/core",
	"version": "1.0.1",
	"description": "Core application framework for nan0web with database handling, internationalization, and interface connectivity",
	"type": "module",
	"main": "src/index.js",
	"types": "types/index.d.ts",
	"files": [
		"src/**/*.js",
		"!src/**/*.test.js",
		"types/**/*.d.ts"
	],
	"scripts": {
		"build": "tsc",
		"clean": "rm -rf .cache/ && rm -rf dist/",
		"clean:modules": "rm -rf node_modules",
		"precommit": "npm test",
		"prepush": "npm test",
		"prepare": "husky",
		"prepublishOnly": "npm test && npm run build",
		"play": "node play/main.js",
		"release": "nan0release publish",
		"test": "node --test",
		"test:docs": "node --test src/README.md.js",
		"test:release": "node --test \"releases/**/*.test.js\"",
		"test:coverage": "node --experimental-test-coverage --test-coverage-include=\"src/**/*.js\" --test-coverage-exclude=\"src/**/*.test.js\" --test \"src/**/*.test.js\"",
		"test:coverage:collect": "nan0test coverage",
		"test:status": "nan0test status --hide-name",
		"test:all": "npm run test && npm run test:docs && npm run build"
	},
	"keywords": [
		"nan0",
		"data",
		"types",
		"utilities",
		"object",
		"array",
		"validation",
		"format",
		"parser"
	],
	"author": "ЯRаСлав YaRaSLove <support@yaro.page>",
	"license": "ISC",
	"packageManager": "pnpm@10.11.0",
	"devDependencies": {
		"@nan0web/release": "1.0.1",
		"@nan0web/test": "1.1.3",
		"husky": "^9.1.7"
	},
	"dependencies": {
		"@nan0web/interface": "1.0.1",
		"@nan0web/protocol": "1.0.0"
	}
}
