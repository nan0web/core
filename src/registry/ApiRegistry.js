/**
 * Simple wrapper for API route registration.
 * Extends the generic RouteRegistry to keep a semantic name.
 */
import RouteRegistry from './RouteRegistry.js'

export default class ApiRegistry extends RouteRegistry { }
