/**
 * Wrapper around ComponentRegistry for UI components.
 * Provides the same API surface (register, getComponent, etc.).
 */
import ComponentRegistry from './ComponentRegistry.js'

export default class UiRegistry extends ComponentRegistry { }
